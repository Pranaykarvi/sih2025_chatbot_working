# Pinned direct dependencies for fast, deterministic installs (minimal pip backtracking).
#
# FastAPI was upgraded from 0.95.x → 0.115.x: google-genai requires Pydantic v2; old FastAPI
# required Pydantic v1 and cannot be installed together with google-genai.
#
# httpx must stay >=0.28.1,<1: google-genai requires httpx>=0.28.1 (do NOT pin 0.27.x — breaks Gemini).
# groq>=0.13 is required for httpx 0.28+ (older groq passed httpx Client(proxies=...) which 0.28 removed).
# Single httpx in the tree: pin it here and avoid duplicate httpx lines elsewhere.

fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

cohere==5.16.0
supabase==2.15.3

PyPDF2==3.0.1
python-docx==1.1.2
cryptography==42.0.8

httpx==0.28.1
tenacity==9.1.3

google-genai==1.46.0
groq==1.1.2
